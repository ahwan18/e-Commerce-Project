import { Catalog as BaseCatalog } from '../Catalog';

export const Catalog = () => {
  return (
    <div>
      {/* Mode 2 Specific header/banners could go here */}
      <BaseCatalog />
    </div>
  );
};
